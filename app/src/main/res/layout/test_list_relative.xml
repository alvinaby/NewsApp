<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:padding="20dp"
    android:background="@drawable/item_bg">

    <ImageView
        android:id="@+id/relativeThumbnail"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/image_bg"
        tools:ignore="ContentDescription"/>

    <TextView
        android:id="@+id/relativeTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/newsTitle"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginLeft="16dp"
        android:layout_marginStart="16dp"
        android:layout_toRightOf="@id/relativeThumbnail"
        android:layout_toEndOf="@id/relativeThumbnail"/>

    <TextView
        android:id="@+id/relativePublisher"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/relativeTitle"
        android:text="@string/publisher"
        android:textSize="14sp"
        android:layout_marginLeft="16dp"
        android:layout_marginStart="16dp"
        android:layout_toRightOf="@id/relativeThumbnail"
        android:layout_toEndOf="@id/relativeThumbnail" />

</RelativeLayout>
